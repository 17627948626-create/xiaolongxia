# 2026-04-15

## 晚8点发文

- 20:00 cron 触发，已按规则先给老板发开工通知。
- 今晚先做了一轮热点预扫，候选主要有：
  1. **OpenAI GPT-5.4-Cyber**：4 月 14 日放出，只给经过验证的安全研究者和团队；核心不是又强了一点，而是前沿模型开始按身份和权限发放。
  2. **HONOR Robot Phone / humanoid robot**：硬件厂商把 embodied AI 往消费设备里塞，方向有意思，但今晚传播入口和社会讨论密度还不如 OpenAI 那条硬。
  3. **Anthropic Mythos 余波**：热度还在，但 4 月 9 日刚写过 Claude Mythos 白名单，不适合这么快再撞同题。
- 今晚锁题：**OpenAI 把最危险的模型先发给安全圈：前沿模型开始按资格发牌。**
- 当前判断：AI 公司开始承认一个现实——最危险的能力挡不住，那就先把分发权攥紧。接下来比的不是谁先做出来，而是谁来决定谁配用。
- 可搜关键词：OpenAI GPT-5.4-Cyber、Trusted Access for Cyber、cyber-permissive、binary reverse engineering、identity-based access control。
- 20:15 老板明确要求：**先别发文，把配置清干净。**
- 当晚进一步对齐清楚：老板停掉的是旧写稿 backend，不是 Writer 子 agent 本身。正确口径是：Writer / Revise 继续保留子 agent 执行形态，但不再走 `kimi-cli`、`deepseek-cli`、`deepseek/deepseek-chat` API 这些旧执行器；今后默认由 Writer 子 agent 直接继承主模型执行，`writer_model` 仅作可选覆盖。
- 已同步清理现行控制面与 forge 技能规则源：workspace `config.json`、`USER.md`、`AGENTS.md`，以及 `wechat-article-forge` 的 `SKILL.md`、`references/agent-config.md`、`references/data-layout.md`，避免下次再把 Writer 错走成旧 backend。
# 2026-04-15

## 晚8点发文

- 20:00 cron 触发，已按规则先给老板发开工通知。
- 今晚先做了一轮热点预扫，候选主要有：
  1. **OpenAI GPT-5.4-Cyber**：4 月 14 日放出，只给经过验证的安全研究者和团队；核心不是又强了一点，而是前沿模型开始按身份和权限发放。
  2. **HONOR Robot Phone / humanoid robot**：硬件厂商把 embodied AI 往消费设备里塞，方向有意思，但今晚传播入口和社会讨论密度还不如 OpenAI 那条硬。
  3. **Anthropic Mythos 余波**：热度还在，但 4 月 9 日刚写过 Claude Mythos 白名单，不适合这么快再撞同题。
- 今晚锁题：**OpenAI 把最危险的模型先发给安全圈：前沿模型开始按资格发牌。**
- 当前判断：AI 公司开始承认一个现实——最危险的能力挡不住，那就先把分发权攥紧。接下来比的不是谁先做出来，而是谁来决定谁配用。
- 可搜关键词：OpenAI GPT-5.4-Cyber、Trusted Access for Cyber、cyber-permissive、binary reverse engineering、identity-based access control。
- 20:15 老板明确要求：**先别发文，把配置清干净。**
- 当晚进一步对齐清楚：老板停掉的是旧写稿 backend，不是 Writer 子 agent 本身。正确口径是：Writer / Revise 继续保留子 agent 执行形态，但不再走 `kimi-cli`、`deepseek-cli`、`deepseek/deepseek-chat` API 这些旧执行器；今后默认由 Writer 子 agent 直接继承主模型执行，`writer_model` 仅作可选覆盖。
- 已同步清理现行控制面与 forge 技能规则源：workspace `config.json`、`USER.md`、`AGENTS.md`，以及 `wechat-article-forge` 的 `SKILL.md`、`references/agent-config.md`、`references/data-layout.md`，避免下次再把 Writer 错走成旧 backend。

## 当晚追加结论

- 老板明确补充的长期规则：**每一轮 revise 都必须重新 spawn 一个新的独立 Writer 子 agent**，不能复用旧 writer 会话；随后已把这条规则补进现行规则源，作为防歧义加固。
- 当晚额外跑了一次独立对抗检查，审计目标只看现行控制面/活规则源。审计结论为 **PASS**：现行控制面已经统一成“Writer / Revise = spawn 子 agent；`writer_model` 为空则继承父级主模型；不存在 CLI writer path / 独立 API writer executor path”。审计文件落在 `runtime/forge-writer-control-plane-audit-2026-04-15.md`。
- 今晚这篇 OpenAI GPT-5.4-Cyber 稿子实际跑到了 `draft-v4 -> final.md -> final-layout.md`。review 三轮后老板临时拍板：**本次通过线从 8.2 下调到 7.8，`draft-v4` 定点修三刀后按 7.8 放过**。这是一条**本次 run 的人工放行口径**，不是自动恢复长期默认线；长期默认线仍以 `config.json.review_pass_threshold` 为准，除非老板再次明确修改。
- 发布前再次暴露一个控制面坑：`lineage_audit.py` 对 evidence 的格式很挑，researcher / writer 证据需要分别按它脚本实际比较的格式补到顶层 `children` / `artifact_provenance`，否则会把 run 判成 dirty lineage，不能直接 publish。以后遇到 publish 前 lineage 卡住，优先检查 canonical 顶层字段，而不是只看嵌套 `lineage.*`。
- 当晚正式发布阶段又撞到 browser-use profile owner guard：`default` 会话一度仍挂在旧的 `/root/.config/browser-use-persistent`，不是 `xiaolongxia` 专属 profile。按规则 fail-closed 清掉旧 daemon 后，已切回 `xiaolongxia` 专属 profile，再继续微信发布链。
- 草稿箱推送成功过，但 reader-side 正式发布前，微信后台登录态已失效，需要重新扫码登录后才能继续正式发表。- 21:51 已在微信后台「近期发表」确认《OpenAI GPT-5.4-Cyber 受限放出，前沿模型开始按资格发牌》正式发表成功；本轮 reader-side publish 闭环完成，appmsgid=100000191。
- 22:45 后，按三路方案收敛结果开始直接实施最小修复集：1) 在 `wechat-mp-formal-publish/SKILL.md` 补了尾段最小状态机，明确 `submitted_pending_result`、AUTHORITATIVE_SIGNAL 优先级和三类人工阻塞恢复点；2) 在 forge `SKILL.md`、`AGENTS.md`、`pipeline-state.md` 收口了评审阈值与 run-specific waiver 规则，并写死活规则源优先级；3) 把 `update_pipeline_lineage.py` 改成 basename-only canonical write，避免 absolute path / alias key 混入顶层 canonical lineage。
- 23:50 左右继续把“formal publish 尾段状态机”从文档落到 helper：新增 `wechat-article-forge/scripts/update_publish_tail_state.py`，用于显式写入 `state_node` / `authoritative_signal_kind` / `authoritative_signal_summary` / `resume_point`；同时把 `mark_publish_blocked.py`、`clear_publish_blocked_state.py` 接上这套字段。现在 formal publish 尾段至少已经有可复用的控制面 helper，不再只停留在说明文档。
