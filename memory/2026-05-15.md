# 2026-05-15

## Health Check / Feishu DM Investigation

- 上午老板反馈“小龙虾主 agent 不回话”。排查结论分两层：
  - 09:24 健康检查 run 失败，不是公众号流程问题；底层模型调用路径报过 `Codex subscription usage limit` 和 `OAuth token refresh failed / refresh_token_reused`。
  - 该失败 run 的 trajectory 已是 error，但 sessions list 里仍残留 `running`，这是 OpenClaw runtime/session index 状态收口问题，后续应修为：`session.ended status=error` 时列表必须显示 failed/error，不能残留 running。
- 进一步澄清：Codex/OAuth 不是整体坏掉，因为当前主会话可正常工作。更准确的问题是 OpenClaw 里存在不同模型调用路径/auth profile：
  - 失败健康检查走到 `provider=codex` / `modelApi=openai-codex-responses` / `authProfile=openai-codex:x15809220109@gmail.com`。
  - 当前正常主会话走 `provider=openai` / `modelApi=openai-responses` / `auth=openai-codex:15809220109@163.com`。
  - 建议后续把小龙虾默认模型路由固定到已验证可用的 `openai-responses` 路径，并清理/禁用异常 auth profile，避免健康检查或显式 session 跑到旧路径。
- Feishu DM 可见回复问题单独成立：普通 assistant final 不会自动投递到飞书，必须显式调用 `message.send`；若已 `message.send`，本轮自然回复应保持 `NO_REPLY`，避免双发。

## GitHub Persona Backup

- 老板准备卸载 OpenClaw，要求把 xiaolongxia agent 人格整体备份到 GitHub。已执行白名单备份，原则是只备份“小龙虾是谁、怎么写、记得什么、怎么工作”，不把 `/root/.openclaw` 整包上传。
- 备份 staging 目录：`/root/xiaolongxia-persona-backup`。
- 远端仓库：`https://github.com/17627948626-create/xiaolongxia.git`。
- 当前 git 状态已确认：`main` clean，`HEAD` 与 `origin/main` 同步，最新提交为 `170fb8a Backup xiaolongxia agent persona`。
- 备份中已特别处理：
  - OpenClaw gateway token、微信后台 URL token 等敏感字段在备份副本中打码。
  - 第一版 staging 曾带入 `artifacts/wechat-rpa` 历史截图；已从提交历史中剔除并重写首个 commit 后推送，避免远端历史残留不必要截图。
- 明文 GitHub 备份定位：人格文件、长期/每日记忆、写作流程资产、文章生产配置、历史稿件和发布记录。登录态、OAuth 状态、browser profile/cookie、session 原始日志、微信/飞书 secret 不进明文仓库。

## Neat Cleanup

- 老板随后要求执行 neat。已按 `neat-freak` 思路做收尾同步：先体检文档尺寸，再优先修正会影响迁移恢复的活事实。
- 本轮未做大规模瘦身：`AGENTS.md` 行数仍在软线内但字节偏大，`MEMORY.md` 已超过软线；临卸载前不做高风险历史压缩，避免误删人格/发布链路关键事实。
- 已同步到 `MEMORY.md` 的长期事实：OpenClaw 当前版本为 2026.5.12；GitHub 私库为人格恢复首选源；Feishu DM 可见回复必须 `message.send`；09:24 健康检查暴露 session error 残留 running 的 OpenClaw 状态收口问题；当前 cron 列表为空，恢复每日发文需重建晚8点 job。
