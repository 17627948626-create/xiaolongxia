# 2026-03-30

## 已发布
- 10:59｜《AI早报｜OpenAI亲手送走Sora，Claude却悄悄学会了开电脑》

## 今天发生的事
- 完成了一轮工作环境自检：cron、skills、发布环境、搜索工具链、workspace 文件都摸清了
- 明确搜索主力改为 Tavily + jj-search-stack，不再依赖 Brave web_search
- 重新梳理了 cron 的定位：它应该是“闹钟”，不是后台派工单
- 发现旧 cron 里存在 sessionTarget 语义不统一、announce 与当前群聊目标不一致的问题
- 已把早8点和晚8点 cron 都收敛到当前这个 Feishu 群聊语义，并强化 prompt：
  - 读 published-logs 最近 5 条
  - 读今天和昨天的 daily memory
  - 不写早报/晚报式资讯拼盘
  - 群里只做一句开工、扫码、最终结果三类必要可见信息

## 晚上任务提醒
- 晚上不要重复早上的 Sora / Claude / OpenAI 结构
- 更适合写一件具体的事，带明确判断
- 发布前要留意 browser-use / 微信后台登录态

## 晚8点执行结果
- 选题：人形机器人感知-行动回路延迟（观点文章）
- 标题：《人形机器人最大的敌人，不是智量，是手速》
- 状态：✅ 已进草稿箱，❌ browser-use session watchdog 30s 超时挂死，自动发布失败
- 已在群里通知老板手动登录公众平台后台完成发布，待老板确认

## 晚9点复盘（2026-03-30）
- 上一轮未结束（草稿箱有文，正式发布待老板确认），本轮只做阻塞复盘，未进入迭代
- 最严重问题：browser-use session watchdog 超时挂死，发布链路完全断开，单点依赖无 fallback
- 复盘深度文件：retrospectives/2026-03-30-evening.md
- 老板提出：除了早8/晚8发文，还要加早9/晚9两个复盘闹钟，专门复盘上一轮发文过程
- 复盘目标不是赶紧给答案，而是深挖问题、卡点、教训，把所有能提升公众号运营能力的信息及时落盘
- 已落地最小方案：新增 `RETRO.md`，新增早9/晚9复盘 cron，新增 `retrospectives/` 目录
- 复盘机制要求：主 agent 保持轻量，重活尽量交给不同模型的子 agent；每轮最多只改一个最严重问题，改动过大就先记录不硬改
- 老板明确偏好高可见性、不怕打扰；因此把 4 条 cron 的 `delivery.mode` 从 `none` 改为 `announce`，并把 prompt 从“少刷屏”调整为“关键进度及时汇报”
- 进一步按“不要过度修改，但能改的都改”做了第二轮最小补强：
  - 写作 cron 强制汇报 4 个关键节点（定题、研究/写作完成、发布中/需扫码、最终结果）
  - 任一环节卡住超过10分钟或需要老板介入，必须立即群里说明
  - 写作轮只有在发布成功/明确失败 + 写完 daily memory 后才算结束
  - 复盘 cron 超时从 7200 收到 3600；若上一轮未结束，只做阻塞复盘，不进入迭代，也不和发文任务抢上下文

## 晚8点文章最终发布结果（21:02）
- 标题：《人形机器人最大的敌人，不是智商，是手速》
- 状态：✅ 已发表（21:02）
- 发布过程：browser-use 0.12.5 watchdog超时 bug（root无--no-sandbox）→ 已修复 sessions.py → 老板扫登录码 + 微信安全验证码 → 发布成功
- browser-use 修复记录：在 skill_cli/sessions.py 的 create_browser_session 加了 chromium_sandbox=False，彻底解决 root 环境下启动失败问题
