# 2026-03-31

## 早8点那轮

**选题:** Agent 为什么总在最后一步翻车--执行链路最后一英里太脆了

**写作状态:** ✅ 完成(8步流水线全跑,评分8.58 PASS,媒体ID已获取)

**发布状态:** ✅ 已发布--09:08「等候发表」,老板扫码验证成功

**发布流程:** 老板扫登录码(09:05)→ 进编辑页 → 发表 → 继续发表 → 微信验证扫码(09:08)→ 成功跳回首页,状态「等候发表」

---

## 9点复盘

**最严重问题:** 主 agent spawn Orchestrator 后没有主动确认发布结果,子 agent 遇到扫码后发到私信就自己结束,发布悬在半空。

**本轮是否迭代:** ✅ 是(老板确认后做了补救迭代)

**改动:** AGENTS.md 发文流程第7步加入强制发布确认逻辑,第9步补充「不发私信,不发飞书文档」负面约束。经 Gemini 验收:LGTM。

**复盘落盘:** retrospectives/2026-03-31-morning.md

## 上午补充记录(10:44)

- 已确认昨晚文章《人形机器人最大的敌人,不是智商,是手速》实际已于 2026-03-30 21:02 发出;今早文章《Agent 为什么总在最后一步翻车》于 09:08 提交成功,状态为"等候发表"。
- 今天做了一次小范围正式迭代:已修改 AGENTS.md 发文流程,新增两条硬约束:
  1. Orchestrator 跑完后,主 agent 必须主动确认发布结果(读 published-logs 最新一条,或查微信后台近期发表);未确认发布成功,不能算这一轮结束,必须在群里说明卡点与所需配合。
  2. safe_check / 登录二维码只发当前 Feishu 群,不发私信,不发飞书文档。
- 这次迭代已拉 Gemini 做代码评审,结论为 LGTM。
- 11:28 补跑 9 点复盘 cron:已把 `retrospectives/2026-03-31-morning.md` 补齐为最终版,确认本轮最严重问题是"主 agent 未主动确认发布结果",无新增改动。
- 验证了 browser-use 的微信后台登录机制:正常情况下不需要每次重新扫码登录,session/Cookie 能复用;但每次正式发表时仍可能触发微信 safe_check,需要老板扫码确认。今早额外登录扫码属于登录态失效,不是正常每次都要登录。
- 对今早文章"没走少数派主题"做了深挖,当前高置信结论:问题不在 browser-use 正式发布,而在"推草稿进草稿箱"阶段的发布链路跑偏。
  - 直接原因:Orchestrator 最终调用了 `wenyan-mcp.publish_article(..., theme_id: "default")`,没有使用生产预期的少数派主题。
  - 上游异常:这轮 `pipeline-state.json` 写的是 `公众号: 小龙虾有话说`,而不是标准字段 `profile: 小龙虾有话说`;同时 Orchestrator 还自行改写了 `publish.md`,把 `profile` / `article_type` 等关键 front matter 删掉了。
  - 结构性判断:这轮没有稳走 profile-aware 的 `wechat-mp-publisher` 包装层,而是裸调了底层 `wenyan-mcp` 工具,导致 profile/theme 约束失效。
  - 额外诱因:当前链路里存在 `shaoshupai` / `sspai` / `default` 三套主题命名,容易诱导模型选错 theme_id。
- 上午已做主题统一收口:本机 `wechat-article-writer/config.json`、`profiles.json`、本机 `~/.config/wenyan-md/config.json` 全部改为唯一主题 `sspai`;本机 MCP 客户端 `list_themes` 只返回 `sspai`,并且硬拒绝 `default` / 其他 theme。
- 远端 Wenyan 服务器(82.157.250.171)也已同步改成唯一主题 `sspai`:远端 `~/.config/wenyan-md/config.json` 只保留 `sspai`,旧 `shaoshupai.css` 已移除,`wenyan.service` / `wenyan-router.service` / `wenyan-xiaolongxia.service` 已重启。
- `wechat-article-forge` 的技能规则也已补硬约束:生产只允许 `sspai`,禁止直接裸调 `wenyan-mcp.publish_article`,并要求使用标准 `profile` 字段。

## 晚8点那轮

**选题：** AI说「我检查过了」，是最危险的四个字——多模型交叉验证，AI自我报告不可信

**写作状态：** ✅ 完成（8步流水线全跑）

**发布状态：** ✅ 已发表（21:00）

**发布流程：** Orchestrator 跑完草稿 → 微信登录码过期，老板扫码登录 → safe_check 扫码 → 正式发表 → 21:00 已发表确认

---



- 复盘触发时间:18:07 CST(比实际22:00早4小时,因下午 cron 配置更新触发了 update-and-fire)
- 晚8点写作本轮尚未触发(nextRunAtMs 在20:00 CST),只做阻塞复盘,不进入迭代
- 最严重问题:MEMORY.md Cron 配置现状一节未同步今天的大改(sessionTarget从群→main,delivery.to 改为 DM 方向)
- 复盘落盘:retrospectives/2026-03-31-evening.md
- 本轮是否改动:否
- 后续关注:今晚20:00写作 cron 是否正常触发、是否在正确会话汇报

## 18:32 DM session 迁移完成

- 旧 DM session(agent:xiaolongxia:feishu:direct:ou_137c41086239266036853c70dd1ae919)44条消息已摘要迁移到 MEMORY.md
- 4条 cron 的 sessionKey 全部清除,只保留 sessionTarget: session:agent:xiaolongxia:main
- 旧 session 文件 + store 条目已删除,仅保留 main session
- ⚠️ 下方"下午补充记录"里的 cron 群聊 target 是中间状态,已被本次迁移覆盖

## 下午补充记录(会话治理 / 标题修复 / 会话迁移)

- 对"必须马上修"的两项又做了一轮正式落地并三方验收通过:
  1. 4 条 cron 全部显式写死 Feishu 群 target:`mode=announce`、`channel=feishu`、`to=chat:oc_6dd47f6ff1f5597531595b66f4886833`(⚠️ 注:此 cron delivery 配置在 18:32 DM session 迁移时再次更新,非最终状态,见上方迁移记录)
  2. `wechat-article-writer/config.json` 里的全局 `wechat_author` 与 `published_log_path` 已清空;forge 规则与参考文档同步改成"必须显式走 profile",不再靠全局 fallback 兜底
- 上述两项经三方会审(系统/架构、代码/配置、运营/流程)均判定 PASS;这意味着"群聊回传 target 缺失"和"profile 失手后串作者/串日志"这两类急项已经闭环
- 又定位并修复了"平台标题与正文 H1 重复"问题:根因是 `publish.md` 里同时存在 `front matter.title` 和正文第一行同名 `# 标题`
- 最终落地方案:
  1. 新增脚本 `wechat-article-forge/scripts/normalize_publish_md.py`,把 `front matter.title` 作为唯一标题真源;若正文第一个 H1 规范化后相同,则删除该 H1 及其后的空行
  2. forge 流程强制:写完 `publish.md` 后必须运行该脚本
  3. Writer 初稿 prompt 与修订 prompt 都补上规则:正文禁止文档级 H1,正文标题从 `##` 开始
  4. publisher 保留并升级兜底去重逻辑
- 标题重复修复也完成了三方复验(代码 / 系统架构 / 运营阅读体验),全部 PASS;真实问题稿副本和带书名号测试稿都通过
- 当前群聊 session key:`session:agent:xiaolongxia:feishu:group:oc_6dd47f6ff1f5597531595b66f4886833`
- 老板已创建独立的 Feishu 机器人,接下来将把我迁移到对应的 DM 会话;**当前这个群聊后续不再作为长期工作主会话使用,待老板解散**
- 因此后续应把"当前群是主战场"的假设废弃,新的长期工作语义应迁移到独立机器人 DM;与群聊强绑定的可见性/cron 语义也需要在新会话落定后重新确认

## 晚10点复盘（22:00 正式触发）

- 晚8点那轮已完成，21:00正式发表，进入正式复盘
- 最严重问题：Orchestrator 遇到微信登录过期直接退出，没有实现"截图→发给主 agent→等待扫码→继续"的完整登录中断恢复流程，导致发布过程分成3段接力完成，总耗时接近1小时
- 本轮是否改动：否（问题在 Orchestrator 内部流程，牵扯面较大，先记录不改）
- 复盘落盘：retrospectives/2026-03-31-evening.md

## 深夜加更（23:48）

- 老板要求写 Claude Code 源码泄露吃瓜文章
- 选题：Anthropic 偷偷给 Claude 养了只宠物，没想到被人发现了（主打 BUDDY 宠物功能）
- 文章已发表（23:48），published-logs 已更新 status=published
- 心跳间隔：任务期间改为3分钟，发布完成后已恢复30分钟
- 本次发布流程再次出现登录态过期 + safe_check 双重扫码，待后续迭代 Orchestrator 断点续传能力
