# 2026-04-08

- 上午和老板一起把昨晚那篇 Anthropic「300 亿美元 + 3.5GW TPU」卡在 8.3 分的稿子重新翻出来，确认断点仍在 review gate，未正式发表。
- 随后做了两轮隔离模型测试：一轮是同题整稿改写对比（`gpt-4o` / `gpt-4.1` / `deepseek/deepseek-chat`），一轮是更聚焦的 B 方案（只测标题 / 开头 / 结尾）。两轮结果都显示：**DeepSeek 最稳，gpt-4.1 次之，gpt-4o 不适合直接扛主稿**；其中 `gpt-4o` 第一轮 B 测试还出现了明显异常落盘（`Lorem/Ipsum` 与乱码），之后又单独补测了一轮，确认链路修好后它仍然只到“usable_with_edits”，不适合做主稿 writer。
- 老板最终拍板三条新口径：1) 写作流水线通过门槛下调，并在后续被收口为 `config.json.review_pass_threshold` 这一个权威源；2) Writer **继续固定使用 `deepseek/deepseek-chat`**；3) `gpt-4.1` 和 `gpt-4o` **以后完全不考虑**。我已同步修改 `wechat-article-writer/config.json`、`USER.md`、`MEMORY.md`。
# 2026-04-08

- 上午和老板一起把昨晚那篇 Anthropic「300 亿美元 + 3.5GW TPU」卡在 8.3 分的稿子重新翻出来，确认断点仍在 review gate，未正式发表。
- 随后做了两轮隔离模型测试：一轮是同题整稿改写对比（`gpt-4o` / `gpt-4.1` / `deepseek/deepseek-chat`），一轮是更聚焦的 B 方案（只测标题 / 开头 / 结尾）。两轮结果都显示：**DeepSeek 最稳，gpt-4.1 次之，gpt-4o 不适合直接扛主稿**；其中 `gpt-4o` 第一轮 B 测试还出现了明显异常落盘（`Lorem/Ipsum` 与乱码），之后又单独补测了一轮，确认链路修好后它仍然只到“usable_with_edits”，不适合做主稿 writer。
- 老板最终拍板三条新口径：1) 写作流水线通过门槛下调，并在后续被收口为 `config.json.review_pass_threshold` 这一个权威源；2) Writer **继续固定使用 `deepseek/deepseek-chat`**；3) `gpt-4.1` 和 `gpt-4o` **以后完全不考虑**。我已同步修改 `wechat-article-writer/config.json`、`USER.md`、`MEMORY.md`。
- 中午又核了一遍最近真实流水线的 `pipeline-state.json`：当前实际模型分工已确认是 **只有 Writer 用 `deepseek/deepseek-chat`，Researcher / Reviewer / Fact-checker / Humanizer / Layout 都还是 `openai-codex/gpt-5.4`**。这个口径来自最近几篇真实 run，不是凭记忆猜的。
- 另外确认了一条执行层待收口事项：历史产物里的旧门槛数字不必回写，但**凡是未来还会被读取的规则文档、模板和未完成草稿里的 `pass_threshold`，都应继续统一到单一权威源 `config.json.review_pass_threshold`**，避免后续恢复执行时又被旧门槛带偏。
- 2026-04-08 晚8点轮热点预扫：候选里最强的三个入口分别是：1) 千寻智能 30 天内两轮累计 30 亿元融资，雷军系顺为与马云系云锋首次在具身赛道“同框”；2) 优必选在巨亏背景下给具身智能首席科学家开出最高 1.24 亿元总包；3) 具身路线开始明显往“数据飞轮 + 商业场景落地”收口。最终决定主写 **千寻智能 30 亿融资** 这条，核心判断是：**资本现在押的已经不是“机器人像不像人”，而是“数据和场景能不能把机器人变成生意”**。
- 这轮晚8稿又真实暴露了一个新的机制问题：**表达层 blocker 没有被稳定翻译成 Writer 可执行改稿合同**。review 连续三轮都在拦同一个模板味问题，但主流程前两次只是把 reviewer 结论转述给 Writer，没有先压成原子改稿合同，导致 Writer 一直在“改内容但没改骨架”。因此我今天补了两条新硬规则：1) 同一 `BLOCKER` 连续两轮未清时，主 agent 必须先把它压成 `blocker_id / why_still_blocking / must_replace / max_keep / preserve / rewrite_budget` 这种原子改稿合同，再允许下一轮 Writer；2) **表达层 blocker** 第二次未清就进入该模式，第三次仍未清则默认停止自动改稿，转主 agent / 老板人工判断，不再同构空转。
- 晚上这篇《AI写作，正在被「像在写」的幻觉吃掉》最终在 16:47 正式发表。过程里顺手查明并修掉了一个更底层的问题：browser-use 的微信持久 profile `/root/.config/browser-use-persistent` 被 `money` 会话占用，导致 `default` 这条发布链路只有 daemon 没有真正接管浏览器，老板扫码多次都写进了错误会话。后面我先定位到占用 PID，再强制清掉 `money/default` 残留 daemon 和持久锁，重建 `default`，这才把 safe_check 跑通并正式发出去。这个教训很值钱：以后微信发布链路出登录态异常，先查**是不是别的 browser-use session 抢了 profile**，不要先怀疑老板没扫成功。
- 发完后我继续把这条隐患彻底收口：新增 browser-use agent registry / wrapper / profile owner guard，并且已经把 `xiaolongxia` 从旧目录 `/root/.config/browser-use-persistent` 真正迁移到独立目录 `/root/.config/browser-use-profiles/xiaolongxia`。迁移后重新拉起 `default`，直接落在 `mp.weixin.qq.com/cgi-bin/home?...`，说明登录态完整带过去了。以后新 agent 要用 browser-use，不能再只约定 session 名不同，必须连 `user_data_dir` 一起分开。
# 2026-04-08

- 上午和老板一起把昨晚那篇 Anthropic「300 亿美元 + 3.5GW TPU」卡在 8.3 分的稿子重新翻出来，确认断点仍在 review gate，未正式发表。
- 随后做了两轮隔离模型测试：一轮是同题整稿改写对比（`gpt-4o` / `gpt-4.1` / `deepseek/deepseek-chat`），一轮是更聚焦的 B 方案（只测标题 / 开头 / 结尾）。两轮结果都显示：**DeepSeek 最稳，gpt-4.1 次之，gpt-4o 不适合直接扛主稿**；其中 `gpt-4o` 第一轮 B 测试还出现了明显异常落盘（`Lorem/Ipsum` 与乱码），之后又单独补测了一轮，确认链路修好后它仍然只到“usable_with_edits”，不适合做主稿 writer。
- 老板最终拍板三条新口径：1) 写作流水线通过门槛下调，并在后续被收口为 `config.json.review_pass_threshold` 这一个权威源；2) Writer **继续固定使用 `deepseek/deepseek-chat`**；3) `gpt-4.1` 和 `gpt-4o` **以后完全不考虑**。我已同步修改 `wechat-article-writer/config.json`、`USER.md`、`MEMORY.md`。
# 2026-04-08

- 上午和老板一起把昨晚那篇 Anthropic「300 亿美元 + 3.5GW TPU」卡在 8.3 分的稿子重新翻出来，确认断点仍在 review gate，未正式发表。
- 随后做了两轮隔离模型测试：一轮是同题整稿改写对比（`gpt-4o` / `gpt-4.1` / `deepseek/deepseek-chat`），一轮是更聚焦的 B 方案（只测标题 / 开头 / 结尾）。两轮结果都显示：**DeepSeek 最稳，gpt-4.1 次之，gpt-4o 不适合直接扛主稿**；其中 `gpt-4o` 第一轮 B 测试还出现了明显异常落盘（`Lorem/Ipsum` 与乱码），之后又单独补测了一轮，确认链路修好后它仍然只到“usable_with_edits”，不适合做主稿 writer。
- 老板最终拍板三条新口径：1) 写作流水线通过门槛下调，并在后续被收口为 `config.json.review_pass_threshold` 这一个权威源；2) Writer **继续固定使用 `deepseek/deepseek-chat`**；3) `gpt-4.1` 和 `gpt-4o` **以后完全不考虑**。我已同步修改 `wechat-article-writer/config.json`、`USER.md`、`MEMORY.md`。
- 中午又核了一遍最近真实流水线的 `pipeline-state.json`：当前实际模型分工已确认是 **只有 Writer 用 `deepseek/deepseek-chat`，Researcher / Reviewer / Fact-checker / Humanizer / Layout 都还是 `openai-codex/gpt-5.4`**。这个口径来自最近几篇真实 run，不是凭记忆猜的。
- 另外确认了一条执行层待收口事项：历史产物里的旧门槛数字不必回写，但**凡是未来还会被读取的规则文档、模板和未完成草稿里的 `pass_threshold`，都应继续统一到单一权威源 `config.json.review_pass_threshold`**，避免后续恢复执行时又被旧门槛带偏。
- 2026-04-08 晚8点轮热点预扫：候选里最强的三个入口分别是：1) 千寻智能 30 天内两轮累计 30 亿元融资，雷军系顺为与马云系云锋首次在具身赛道“同框”；2) 优必选在巨亏背景下给具身智能首席科学家开出最高 1.24 亿元总包；3) 具身路线开始明显往“数据飞轮 + 商业场景落地”收口。最终决定主写 **千寻智能 30 亿融资** 这条，核心判断是：**资本现在押的已经不是“机器人像不像人”，而是“数据和场景能不能把机器人变成生意”**。
- 这轮晚8稿又真实暴露了一个新的机制问题：**表达层 blocker 没有被稳定翻译成 Writer 可执行改稿合同**。review 连续三轮都在拦同一个模板味问题，但主流程前两次只是把 reviewer 结论转述给 Writer，没有先压成原子改稿合同，导致 Writer 一直在“改内容但没改骨架”。因此我今天补了两条新硬规则：1) 同一 `BLOCKER` 连续两轮未清时，主 agent 必须先把它压成 `blocker_id / why_still_blocking / must_replace / max_keep / preserve / rewrite_budget` 这种原子改稿合同，再允许下一轮 Writer；2) **表达层 blocker** 第二次未清就进入该模式，第三次仍未清则默认停止自动改稿，转主 agent / 老板人工判断，不再同构空转。
- 晚上这篇《AI写作，正在被「像在写」的幻觉吃掉》最终在 16:47 正式发表。过程里顺手查明并修掉了一个更底层的问题：browser-use 的微信持久 profile `/root/.config/browser-use-persistent` 被 `money` 会话占用，导致 `default` 这条发布链路只有 daemon 没有真正接管浏览器，老板扫码多次都写进了错误会话。后面我先定位到占用 PID，再强制清掉 `money/default` 残留 daemon 和持久锁，重建 `default`，这才把 safe_check 跑通并正式发出去。这个教训很值钱：以后微信发布链路出登录态异常，先查**是不是别的 browser-use session 抢了 profile**，不要先怀疑老板没扫成功。
- 发完后我继续把这条隐患彻底收口：新增 browser-use agent registry / wrapper / profile owner guard，并且已经把 `xiaolongxia` 从旧目录 `/root/.config/browser-use-persistent` 真正迁移到独立目录 `/root/.config/browser-use-profiles/xiaolongxia`。迁移后重新拉起 `default`，直接落在 `mp.weixin.qq.com/cgi-bin/home?...`，说明登录态完整带过去了。以后新 agent 要用 browser-use，不能再只约定 session 名不同，必须连 `user_data_dir` 一起分开。
- 深夜补记两条 durable 结果：1) DeepSeek 网页端 `Instant vs Expert` 第一阶段 2v2 测试**已按老板要求暂停**，保留临时目录与现有产物，不再继续跑新样本；当前优先级切换到基础设施排障，重点只看 **XPS 在线稳定性、DeepSeek 网页长 prompt 提交稳定性、执行链路稳定性**。2) 这次暂停不是否定测试问题本身，而是明确收口：后续若重启测试，只允许换执行链路/技术实现，不得改测试目标、输入包、2v2 矩阵与盲评方法。
- 另一个很值钱的正向验证是：**方案 A（保留 Writer 子代理壳、内部改用 `deepseek-cli`）已经在正式 draft 目录里跑通了一次真实 revision 闭环试点**。目标目录是 `wechat-article-writer/drafts/20260408-2000-lei-jun-ma-yun-jushen-shengyi/`，从 `draft-v2.md + review-v2.json` 出发，成功留下了正式 `draft-v3.md`、`draft-v3.md.backend.json`、`draft-v3.md.prompt.txt`、`writer-lite-check-v3.json`、`review-v3.json`，并把 `pipeline-state.json` 的 `children / artifact_provenance / last_draft_file / last_review_file / lite_preflight` 等字段回写到了正式状态里。这个结果说明：**Writer backend 切成 `deepseek-cli` 不需要把正文生成挪回主 agent，正式 child/provenance 证据链是能保住的。**
- 这次 deepseek-cli revision 试点也顺手暴露了一个很具体的执行层坑：最初失败点不是写作逻辑，而是 **子代理 shell 里没有 `python`，只有 `python3`**。后面修正执行链路后才真正跑通。因此以后如果继续把 Writer backend 切到脚本调用链，第一时间先查 runtime 里的解释器名和 PATH，不要把这种环境错误误判成模型/流程问题。
- 同一轮试点的内容结论也值得记：虽然 deepseek-cli backend 真跑通了，但这版 `review-v3.json` 依然是 **`decision=revise`、`weighted_total=8.1`**，主要未清 blocker 还是**固定对比句骨架过密、模板味压住作者感**。也就是说，这次验证通过的是“执行链路/证据链”，不是“稿子质量已经自动过线”。
