# 2026-04-09

- 00:04，用户指出《AI写作，正在被「像在写」的幻觉吃掉》已发表版本有问题后，我按要求从 review 重新走后半段 forge：review-v6（pass, 8.4）→ fact-check-v2（approve, safe_to_publish=true）→ humanized-v2 → final-layout-v2 → 重推草稿箱 → 微信 safe_check 扫码 → 微信后台“近期发表”确认 **已发表**。旧的 2026-04-08 16:47 那条后台记录显示为“已删除”，新的正式发表时间以 2026-04-09 00:04 为准。

## 下午：forge 主链重构闭环完成

- 老板下午确认了新的 forge 收口方向，并要求走完整多 agent 闭环，但同时明确提醒：**目标优先，不要把事情做过**；凡是鸡蛋里挑骨头、过度思考、简单问题复杂化的提议，都应由主控直接否掉。
- 我按这个原则跑完了一轮完整闭环：Phase 0 定范围 → 3 路调查员并行查规则源 / writer 链 / layout-publish 链 → 3 路方案评审 → 1 名独立实施员落地 → 2 名判官结构化验收 → 发现真实 FAIL 后按“原子修复合同”只回炉修必要点 → 再次复验直到 PASS。
- 这轮最终收口的现行口径是：1) **review 只看 `weighted_total` 单门**，严重问题通过评分与 `critical_issues` 表达，不恢复 blocker gate；通过门槛数字只认 `wechat-article-writer/config.json` 里的 `review_pass_threshold`；2) **revise 最多 2 次**，不过线就从 fresh first-draft branch 重开；3) Writer 默认链改成 **`kimi-cli`**，`deepseek/deepseek-chat` 只保留为 API fallback；4) **`deepseek-cli` 与 fact-check 都退出 forge 主链**；5) Layout 放宽为 **render adapter**，允许识别隐性标题、重点、亮点、扫描结构与微信安全排版，但**不得改 thesis / facts / arguments / voice**。
- 这轮也顺手补齐了几个会造成“表面收口、实际回旧世界观”的硬点：workspace 活配置已切到 Kimi 默认；`lineage_audit.py` / `update_pipeline_lineage.py` 已升级为不再要求 fact-check 的 `lineage-v2`；`publish_profile_preflight.py` 默认配置路径已对齐到 `workspace-xiaolongxia`；`SKILL.md` 的当前默认路径口径也已同步，不再把人带回旧 `/root/.openclaw/workspace/...`。
- 刻意**没有**做的事：没有搞 repo-wide 历史大扫除，没有新增 verifier/checker/meta-layer，没有顺手重构整套 writer helper 架构；这些都按老板要求压住了，只改会继续影响当前主链行为、当前合同和当前主控认知的那一小撮硬点。
- 傍晚老板又补了一刀很关键的执行层要求：**评分通过门槛数字只能有一个权威源**。我随后做了全量扫描，把活规则源、workspace 当前口径和当天记忆里的硬编码门槛数字都清掉，统一改成只引用 `wechat-article-writer/config.json` 里的 `review_pass_threshold`；以后要改门槛，只改这一个字段。
- 15:27 左右，我顺手检查了当前真实生效的 cron，确认 Gateway 里现在一共 3 条：小龙虾晚 8 点写作、main 的 AI 早报 9AM、AI 晚报 9PM。其中需要改的是小龙虾这条晚 8 点写作 cron：旧 payload 里还显式枚举了 `Fact-checker` 等已过时链路。已将该 cron prompt 收短为“按 AGENTS.md、当前活配置与当前状态文件执行当前主流程”，不再在 cron 文案里复述具体子步骤，避免 forge 再改后 cron prompt 继续过期。
# 2026-04-09

- 00:04，用户指出《AI写作，正在被「像在写」的幻觉吃掉》已发表版本有问题后，我按要求从 review 重新走后半段 forge：review-v6（pass, 8.4）→ fact-check-v2（approve, safe_to_publish=true）→ humanized-v2 → final-layout-v2 → 重推草稿箱 → 微信 safe_check 扫码 → 微信后台“近期发表”确认 **已发表**。旧的 2026-04-08 16:47 那条后台记录显示为“已删除”，新的正式发表时间以 2026-04-09 00:04 为准。

## 下午：forge 主链重构闭环完成

- 老板下午确认了新的 forge 收口方向，并要求走完整多 agent 闭环，但同时明确提醒：**目标优先，不要把事情做过**；凡是鸡蛋里挑骨头、过度思考、简单问题复杂化的提议，都应由主控直接否掉。
- 我按这个原则跑完了一轮完整闭环：Phase 0 定范围 → 3 路调查员并行查规则源 / writer 链 / layout-publish 链 → 3 路方案评审 → 1 名独立实施员落地 → 2 名判官结构化验收 → 发现真实 FAIL 后按“原子修复合同”只回炉修必要点 → 再次复验直到 PASS。
- 这轮最终收口的现行口径是：1) **review 只看 `weighted_total` 单门**，严重问题通过评分与 `critical_issues` 表达，不恢复 blocker gate；通过门槛数字只认 `wechat-article-writer/config.json` 里的 `review_pass_threshold`；2) **revise 最多 2 次**，不过线就从 fresh first-draft branch 重开；3) Writer 默认链改成 **`kimi-cli`**，`deepseek/deepseek-chat` 只保留为 API fallback；4) **`deepseek-cli` 与 fact-check 都退出 forge 主链**；5) Layout 放宽为 **render adapter**，允许识别隐性标题、重点、亮点、扫描结构与微信安全排版，但**不得改 thesis / facts / arguments / voice**。
- 这轮也顺手补齐了几个会造成“表面收口、实际回旧世界观”的硬点：workspace 活配置已切到 Kimi 默认；`lineage_audit.py` / `update_pipeline_lineage.py` 已升级为不再要求 fact-check 的 `lineage-v2`；`publish_profile_preflight.py` 默认配置路径已对齐到 `workspace-xiaolongxia`；`SKILL.md` 的当前默认路径口径也已同步，不再把人带回旧 `/root/.openclaw/workspace/...`。
- 刻意**没有**做的事：没有搞 repo-wide 历史大扫除，没有新增 verifier/checker/meta-layer，没有顺手重构整套 writer helper 架构；这些都按老板要求压住了，只改会继续影响当前主链行为、当前合同和当前主控认知的那一小撮硬点。
- 傍晚老板又补了一刀很关键的执行层要求：**评分通过门槛数字只能有一个权威源**。我随后做了全量扫描，把活规则源、workspace 当前口径和当天记忆里的硬编码门槛数字都清掉，统一改成只引用 `wechat-article-writer/config.json` 里的 `review_pass_threshold`；以后要改门槛，只改这一个字段。
- 15:27 左右，我顺手检查了当前真实生效的 cron，确认 Gateway 里现在一共 3 条：小龙虾晚 8 点写作、main 的 AI 早报 9AM、AI 晚报 9PM。其中需要改的是小龙虾这条晚 8 点写作 cron：旧 payload 里还显式枚举了 `Fact-checker` 等已过时链路。已将该 cron prompt 收短为“按 AGENTS.md、当前活配置与当前状态文件执行当前主流程”，不再在 cron 文案里复述具体子步骤，避免 forge 再改后 cron prompt 继续过期。

## 傍晚：晚8点轮 Anthropic Mythos 稿进入正式发表前阻塞

- 晚8点轮（手动触发）选题最终锁为 **Anthropic / Claude Mythos 不公开发、只进白名单**，核心判断是：**前沿模型竞争正在从“谁先发布”变成“谁敢不发布、谁能决定最危险能力先给谁”**。对应草稿目录：`/root/.openclaw/workspace-xiaolongxia/wechat-article-writer/drafts/20260409-2000-anthropic-mythos-bu-gan-fa/`。
- 这轮主链实跑结果：Research 过 gate → 首稿 `draft.md` → lite preflight 通过 → `review-v1.json` 只给 **7.13**，主因是标题关键词不够硬、提示牌式过桥句偏多、后半段抽象化；随后我按 **rewrite-contract-v1.json** 压成定向改稿合同回炉，`draft-v2.md` 经二审 `review-v2.json` **8.27 pass**，再走 Humanizer 与 Layout，`final-layout.md` 已完成。
- 发布侧进度：已完成 lineage audit，`publish.md` 已生成并推入微信草稿箱，`pipeline-state.json` 已落盘 `draft_box_saved=true`，当前 `media_id` 为 `7NYHnQxN70EpaLx4Jh0kyg4x2xlEG_ROphdFRvlTkT7qrcw6mAgQRoVJCV93B2en`。
- 这轮在正式发表前暴露了一个很具体的操作纪律问题：**我曾把“不是二维码的截图”发给老板**。老板当场指出“你给的不是二维码，发二维码截图前自己先看一下”。这条教训必须记住：**以后凡是要发微信扫码图，先自己肉眼确认图里真有二维码，再发；不能把登录页、空白页或错误截图当二维码转发。**
- 随后我自己恢复了微信后台登录态，不再需要老板扫码；但在真正点击群发时，又遇到微信后台自己的平台侧阻塞：**原创校验超时** + **“正在增加群发次数，请于 5 分钟左右后再尝试群发”**。截至 17:00，这篇文章状态是：**草稿箱已保存，但 reader-side 尚未确认 published / in_review，仍需后续重试正式发表**。
- 还有一个控制面修正点：当前 `pipeline-state.json` / `evening-run.lock.json` 里还残留着先前的 `waiting_login_scan` blocked state，但真实卡点已经切到**微信平台冷却/限流**，不是老板操作。后续恢复这轮时，**先把 blocked state 改写成平台侧重试等待**，避免心跳或恢复逻辑继续误判成还在等老板扫码。

- 17:18，晚8点轮《Anthropic 把 Claude Mythos 锁进白名单：最强模型不公开发，只先给少数人用》在微信后台“近期发表”确认 **已发表**。这轮正式发布阶段的关键教训有两条：1) 老板明确要求“发送时别勾选群发”，照做后实际可走 **公众号主页发表** 路径，不必硬走群发；2) 发二维码前必须先自己验图，不能把非二维码截图发出去。本轮最终 appmsgid=`100000163`，safe_check 扫码后闭环成功。
