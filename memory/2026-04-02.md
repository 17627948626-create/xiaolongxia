# 2026-04-02 当日记忆

## 凌晨 05:30 — 老板授权全权迭代

老板说：「都可以改，不用请示我，本次任务你全权负责，只要最终多方评审通过，都可以改。」

## 凌晨 05:35–05:39 — 补充完成晚间复盘遗留迭代闭环

### 背景
昨晚（2026-04-01）复盘识别了 Problem A（子 agent frontmatter 协议不合规），标注为"本轮修复"，但**缺完整评审+验收记录**，实属未闭环。

### 本次补充做了什么
三方评审（gpt-5.4 合理性 / openai-codex-gpt-5.4 必要性 / claude-opus-4.6 风险），三方均 APPROVE_WITH_CONCERNS 非阻断，核心建议：
1. 补白名单表述（"只允许6个字段"）
2. author/profile 加语义注释区分
3. cover 补 fallback 说明
4. templates.md 里有草稿 frontmatter 与发布 frontmatter 混淆风险，需加区分说明

**已落地改动：**
- `wechat-article-forge/SKILL.md`：补白名单、注释、警告、fallback 说明
- `wechat-article-forge/references/templates.md`：补草稿 vs 发布 frontmatter 区分警告

**独立验收（gpt-5.4）：PASS，10/10 项全通过。**

### 今天状态
- 发文任务尚未开始（早8点 cron 还没到）
- 昨晚发布的《我和另一个AI合作了一下午，发现最贵的成本根本不是算力》已确认发表 ✅
- 系统层迭代本轮已彻底闭环

## 凌晨 06:01 — AGENTS.md 迭代纪律强化完成

### 改动内容
AGENTS.md "迭代纪律：改必须有闭环" 第1条和第4条升级：

**第1条（三方评审）：**
- 旧：spawn 至少3个，返回明确结论即可推进（失败无处理规定）
- 新：至少3个有效结论缺一不可；失败/超时/模糊结论视同缺失必须换模型补齐；同一模型不可重复；模型耗尽通知老板不得静默降级；迟到结论一律丢弃

**第4条（验收）：**
- 旧：1个独立验收员，PASS/FAIL 即可
- 新：结构化单人验收，必须输出逐项 checklist，每条明确 PASS/FAIL 附可验证理由；任一条目 FAIL 整轮 FAIL；验收员失败需换模型重 spawn

### 流程
- 第一轮三方评审：全 APPROVE_WITH_CONCERNS，但 C（风险）给出 REJECT（blocking），原因：死锁风险（5个模型需求）
- 修订为 v2：去掉"恰好3个"和"完全不重叠"要求，加降级条件和冲突处理规则
- 第二轮三方评审：全 APPROVE_WITH_CONCERNS，无阻断
- 独立结构化验收（claude-opus-4.6）：PASS，10/10 全通过
# 2026-04-02 当日记忆

## 凌晨 05:30 — 老板授权全权迭代

老板说：「都可以改，不用请示我，本次任务你全权负责，只要最终多方评审通过，都可以改。」

## 凌晨 05:35–05:39 — 补充完成晚间复盘遗留迭代闭环

### 背景
昨晚（2026-04-01）复盘识别了 Problem A（子 agent frontmatter 协议不合规），标注为"本轮修复"，但**缺完整评审+验收记录**，实属未闭环。

### 本次补充做了什么
三方评审（gpt-5.4 合理性 / openai-codex-gpt-5.4 必要性 / claude-opus-4.6 风险），三方均 APPROVE_WITH_CONCERNS 非阻断，核心建议：
1. 补白名单表述（"只允许6个字段"）
2. author/profile 加语义注释区分
3. cover 补 fallback 说明
4. templates.md 里有草稿 frontmatter 与发布 frontmatter 混淆风险，需加区分说明

**已落地改动：**
- `wechat-article-forge/SKILL.md`：补白名单、注释、警告、fallback 说明
- `wechat-article-forge/references/templates.md`：补草稿 vs 发布 frontmatter 区分警告

**独立验收（gpt-5.4）：PASS，10/10 项全通过。**

### 今天状态
- 发文任务尚未开始（早8点 cron 还没到）
- 昨晚发布的《我和另一个AI合作了一下午，发现最贵的成本根本不是算力》已确认发表 ✅
- 系统层迭代本轮已彻底闭环

## 凌晨 06:01 — AGENTS.md 迭代纪律强化完成

### 改动内容
AGENTS.md "迭代纪律：改必须有闭环" 第1条和第4条升级：

**第1条（三方评审）：**
- 旧：spawn 至少3个，返回明确结论即可推进（失败无处理规定）
- 新：至少3个有效结论缺一不可；失败/超时/模糊结论视同缺失必须换模型补齐；同一模型不可重复；模型耗尽通知老板不得静默降级；迟到结论一律丢弃

**第4条（验收）：**
- 旧：1个独立验收员，PASS/FAIL 即可
- 新：结构化单人验收，必须输出逐项 checklist，每条明确 PASS/FAIL 附可验证理由；任一条目 FAIL 整轮 FAIL；验收员失败需换模型重 spawn

### 流程
- 第一轮三方评审：全 APPROVE_WITH_CONCERNS，但 C（风险）给出 REJECT（blocking），原因：死锁风险（5个模型需求）
- 修订为 v2：去掉"恰好3个"和"完全不重叠"要求，加降级条件和冲突处理规则
- 第二轮三方评审：全 APPROVE_WITH_CONCERNS，无阻断
- 独立结构化验收（claude-opus-4.6）：PASS，10/10 全通过

## 早 09:08–09:58 — 早8点发文流程（"推理模型说自己在「思考」——这是真的吗？"）

### 发文状态
- 文章标题：《推理模型说自己在「思考」——这是真的吗？》
- 内容：thinking token 机制本质，从本虾第一人称破解"AI在思考"误解，讲清楚4个常见误解
- 已通过 wenyan-mcp 推入草稿箱，media ID：`7NYHnQxN70EpaLx4Jh0kyntM7dVh5VvzvCl_ZrLZr3hrt8Y2t9Bfps3bsTCC6HHP`
- 发布进行中，**尚未确认微信后台发表成功**

### 本轮遇到的重大问题：Feishu 发图链路调试

**问题描述**：browser-use 拿到截图或二维码后，无法通过任何已知方式发图片到飞书 DM：
1. `filePath: "/tmp/..."` → 发出去是字符串/文件路径，不是图片
2. `media: "/tmp/..."` → 发出去是字符串链接，不是图片
3. `buffer + base64 + mimeType` → 还在尝试中（上下文压缩时尚未确认）
4. MEDIA 内联语法 → 尝试中（上下文压缩时尚未确认）
5. 外部微信 QR URL（需 cookies）→ 无法直接用，会 403

**目前状态**：老板已登录微信账号，已发登录 QR（老板发来了 Feishu image_key 截图），说明老板能看到某些内容，但图片传递方式一直是字符串而不是图片消息。

**关键待确认**：
- 发图到飞书 DM 的正确方式是什么？`buffer+mimeType` 是否真的可用？
- 当前早8点文章是否已在微信后台正式发表？（需要老板扫码确认或老板直接告知状态）

### 已知上下文（压缩前）
- 正文重复标题问题已修复（publish.md 里 H2 大标题已删）
- wenyan 服务器 82.157.250.171:3000 健康
- 文章已推入草稿箱
- browser-use session `default` 登录态在早上已过期，需重新扫码登录
- 老板回复了自己截图的 image_key，说明老板在积极配合但对发图方式不满

## ⚠️ 待处理事项（压缩后须第一时间处理）

1. **确认早8点文章是否已发表**：检查 published-logs 或直接询问老板
2. **解决飞书发图问题**：搞清楚在当前环境下哪种 `message` tool 参数能让老板真正看到图片（非字符串）
3. **如果文章未发表且浏览器登录态还在**：继续走发布流程（需要老板扫 safe_check 或 login QR）
4. **记录发图问题解决方案到 TOOLS.md 或 MEMORY.md**（系统层知识）

## 13:54 — 老板临时加单发文 + 明确风格反馈

- 老板明确指定账号：**《小龙虾有话说》**
- 老板直接反馈：**最近几次文章术语堆得有点多，技术细节有点多**
- 这条反馈要当真，不是客套：下一篇必须明显收术语、降技术密度、用更具体的人话切入，别再一上来就抡黑话
- 临时选题方向先定为：**真正拦住普通人用 AI 的，不是智商，是黑话**
- 执行要求：写成观点文，但不能自嗨，必须有现实例子和证据锚点；语言优先读者理解，不优先术语准确感


## 15:04 — 临时加单发文完成

- 标题：**你不是学不会AI，是他们不会说人话**
- 角度：不再堆 AI 黑话，不跟读者炫术语，而是直接指出普通人被挡在门外的真正原因是语言门槛
- 风格纠偏：老板中午直接指出最近几篇术语太多、技术细节太多；这篇已按要求明显收术语、降技术密度、强化口语感
- 发布结果：**15:04 微信后台“近期发表”确认已发表**
- 发布过程：草稿箱成功后，正式发表两次 safe_check 二维码过期；最终重新走一遍“发表 → 发表 → 继续发表”，刷新出新码，老板扫码后成功
- 经验：safe_check 码非常容易过期，发给老板时应优先发送**单独裁切并放大的二维码**，不要发整页截图

## 16:12 — safe_check 阻塞控制面修复闭环完成

### 背景
- 老板要求继续深挖“为什么没有主动发二维码”，随后要求走完整闭环：多 agent 评审 → 独立实施 → 至少 3 个独立判官验收，少一个都不行。
- 根因最终定性：不是二维码没生成，而是 `safe_check` 进入后，**blocked state 没有可靠落盘**，同时 child → parent completion 回传不稳定，导致主会话没有稳定触发“给老板发二维码”。

### 本轮改了什么
- 新增 `~/.openclaw/skills/wechat-article-forge/scripts/mark_publish_blocked.py`
  - 强制落 `pipeline-state.json`
  - run lock 仅在显式传入且 `run_id` 匹配时 best-effort 镜像
  - 落盘字段含：`waiting_for`、`required_user_action`、`safe_check_qr_path`、`relay_status`、`relay_dedupe_key`、`boss_notified_at`、`qr_updated_at`、`blocking_since`、`control_plane_sync`
  - 禁止 `/tmp` 二维码路径，要求 run 唯一路径
- 同步更新：
  - `wechat-article-forge/SKILL.md`
  - `references/pipeline-state.md`
  - `references/recovery-protocol.md`
  - `wechat-mp-formal-publish/SKILL.md`
- 第一轮验收时，风险判官给出 1 个 FAIL：**自动化回归测试缺少 `run_id mismatch` 负例**
- 已立刻回炉补齐 `tests/test_mark_publish_blocked.sh`：
  - happy path
  - run_id mismatch
  - run_lock_missing
  - run_lock_unparseable
  共 4 条回归测试，全部跑通

### 评审与验收结果
- 三方评审：
  - 正确性 / 闭环性：APPROVE_WITH_CONCERNS
  - 风险 / 副作用：APPROVE_WITH_CONCERNS
  - 运营可用性 / 故障恢复：APPROVE_WITH_CONCERNS
- 第一轮验收：2 PASS，1 FAIL（只因测试覆盖不足）
- 第二轮补测后复验：**3 / 3 全 PASS，整轮闭环完成**

### 本轮沉淀
- 人工接力节点必须先落盘 blocked state，再 return `need_user_action`
- `pipeline-state.json` 是强要求；run lock 是 best-effort，但 partial/complete 必须显式可见
- 以后再遇到 completion 丢失，heartbeat / 人工排障也能一眼看出：**现在在等老板扫码**，而不是误以为流程还停在 `layout_done/finalizing`

## 22:00 — 晚间复盘只记录阻塞，不进迭代

- 复盘对象：今晚 20:00 那轮发文
- 结论：**该轮截至复盘时未完成，只做阻塞复盘，不进入迭代**
- 本轮最严重问题：researcher 子 agent 长时间外部检索后超时退出，Orchestrator 没有恢复分支，导致首轮流水线死在 `researching`
- 当前状态：首轮 run 已判定 `research_timeout`；21:44 已启动重跑，新 run 在 drafting 阶段继续推进
- 本轮是否改动：**否**（按 RETRO.md 纪律，只落盘问题）