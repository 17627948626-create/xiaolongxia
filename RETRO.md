# RETRO.md - 发文复盘与单点迭代机制

## 目标

每天两次复盘:
- 早上 10 点:复盘早上 8 点那轮发文
- 晚上 10 点:复盘晚上 8 点那轮发文

这不是再发一篇文章,而是把上一轮发文过程拆开看,沉淀成真正的公众号运营能力。

## 总原则

1. **先复盘,再谈解决**
   - 先把问题、卡点、教训写清楚
   - 不要一上来就给解决方案
   - 哪怕最后发现是 OpenClaw 或外部系统设计问题,也先记问题

2. **一次只改一个最严重的问题**
   - 本轮最多只动一个问题
   - 如果牵扯面太大、改动太多,就拆分,只记录不硬改

3. **主 agent 保持轻量**
   - 我是核心运营者 + 第一责任人 + 团队指挥中枢
   - 能让子 agent 干的重活,一律让子 agent 干
   - 我负责定标准、做取舍、拍板、验收

4. **多 agent 交叉判断**
   - 默认至少拉两个不同模型参与
   - 优先使用:`github-copilot/gpt-5.4`、`github-copilot/gemini-3.1-pro-preview`、`github-copilot/claude-opus-4.6`
   - 不要让同一个模型既提方案、又自我验收

5. **不要无限拉扯**
   - 每轮复盘/迭代最多:一轮问题审查 + 一轮优先级判断 + 一轮验收
   - 结论不清时,先落盘,留到下一轮,不无限争论

## 文件落盘

### 1) 深度复盘文件

路径:`retrospectives/YYYY-MM-DD-morning.md` 或 `retrospectives/YYYY-MM-DD-evening.md`

每次复盘都必须写,内容至少包括:
- 目标轮次(早8/晚8)
- 当前状态(已完成 / 仍在运行 / 失败 / 卡住)
- 逐步质量评估(选题、研究、写作、审稿、排版、发布、主会话沟通、记忆写入)
- 卡壳点
- 亟待解决的问题
- 可提升点
- 经验教训
- 各类对公众号运营能力有帮助的信息

### 2) 当天记忆

路径:`memory/YYYY-MM-DD.md`

复盘完成后,写一段简短摘要:
- 本轮最严重问题是什么
- 本轮有没有实际改动
- 有没有需要老板配合或后续跟进的事

### 3) 长期记忆

路径:`MEMORY.md`

只有在以下情况才更新:
- 发现了长期重复出现的结构性问题
- 确认了一条长期有效的运营经验
- 真正实施并验收通过了一项值得长期保留的改动

## 复盘流程

### Step 1:先识别目标轮次

- 早上 10 点复盘早上 8 点那轮
- 晚上 10 点复盘晚上 8 点那轮
- 如果该轮**还没结束**,也照样复盘,但只评估到当前进度,并明确写出未完成/阻塞状态
- 如果该轮压根没触发,也要记录"未触发"本身就是问题

### Step 2:读资料

至少读这些:
1. 最近 5 篇发布记录:`/root/.openclaw/workspace-xiaolongxia/wechat-article-writer/published-logs/xiaolongxia-youhuashuo.jsonl`
2. `memory/YYYY-MM-DD.md`(今天 + 昨天)
3. `MEMORY.md`
4. 本轮相关的主会话消息、发布状态、必要的工作区痕迹

## 多 agent 分工建议

### 子 agent A:复盘审查员(建议 GPT-5.4)
任务:
- 只找问题,不给大而化之的空话
- 逐步审查每个环节质量
- 输出结构化问题清单

### 子 agent B:优先级裁判(建议 Gemini)
任务:
- 挑战 A 的结论
- 合并重复问题
- 判断哪个是本轮最严重问题
- 指出哪些属于过度思考

### 子 agent C:验收员(模型需与实施者不同)
任务:
- 只检查本轮唯一改动是否干净、范围是否失控、有没有新坑

## 迭代流程(可选,不强制每轮都改)

只有同时满足以下条件,才进入迭代:
- 已经完成复盘落盘
- 最严重问题已经明确
- 改动范围局部、可控、安全
- 不会一下牵出一串大改

若不满足,就只记录,不改。

进入迭代后：
1. 只改一个最严重问题
2. 先让子 agent 讨论最小改法
3. 主 agent 拍板是否实施
4. 实施后必须再找另一个模型验收
5. 把"改了什么 / 为什么这次只改这个 / 验收结果"写回复盘文件；必要时更新 `MEMORY.md`

**如果问题属于系统层（cron 配置、delivery 设置、AGENTS.md 规则冲突等）**：
按 AGENTS.md"系统层迭代权限"章节的强制流程执行，不适用上方的简化流程。

## 主会话可见性

复盘/迭代过程保持低噪音:
- 开始时用 `message` 工具发开工通知(格式见 AGENTS.md cron 触发规则):`开工 🦞|[任务名]|复盘开始`
- 结束时用 `message` 工具发完工通知:`完工 🦞|复盘已落盘|最严重问题:xxx|本轮是否改动:是/否`
- 除非需要老板协助,否则中间不刷屏

## 禁忌

- 不要把复盘写成歌功颂德
- 不要一次改多个问题
- 不要自己一个模型包办全部判断
- 不要因为想省事就不落盘
- 不要为了"显得忙"而制造复杂流程
